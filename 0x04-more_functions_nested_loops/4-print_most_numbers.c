#include "main.h"
/**
 * print_most_numbers - hello
 *
 * Return: yes return
 */
void print_most_numbers(void)
{
	int i = 0;

	while(i <= 0 )
	{
		if(!(i == 2 || i == 4))
		{
			_putchar(c);
		}
		i++;
	}
}
